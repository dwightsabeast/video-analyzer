# Video Analyzer - Python dependencies
# Required:
opencv-python>=4.5
numpy>=1.20

# Optional (drag-and-drop file open in the GUI):
# tkinterdnd2

# Build-time only (run build.bat to make a standalone video-analyzer.exe):
# pyinstaller

# External binaries (NOT pip-installable - place beside the scripts or on PATH):
#   ffmpeg + ffprobe   (REQUIRED for full functionality; gyan.dev "full" build recommended)
#   dovi_tool          (optional - Dolby Vision RPU extraction)
#   hdr10plus_tool     (optional - HDR10+ dynamic metadata extraction)
